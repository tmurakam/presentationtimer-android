buildscript {
    repositories {
        google()
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:4.0.0'

        classpath 'com.google.gms:google-services:4.3.3' // firebase
        classpath 'com.google.firebase:firebase-crashlytics-gradle:2.1.1' // firebase crashlytics
    }
}

allprojects {
    repositories {
        google()
        jcenter()
    }
}
