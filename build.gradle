buildscript {
    repositories {
        google()
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:4.1.3'

        classpath 'com.google.gms:google-services:4.3.5' // firebase
        classpath 'com.google.firebase:firebase-crashlytics-gradle:2.5.2' // firebase crashlytics
    }
}

allprojects {
    repositories {
        google()
        jcenter()
    }
}
